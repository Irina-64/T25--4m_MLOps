Яковенко Максим: Подготовил и проверил `docker-compose.yaml` и `.env`, инициализировал БД (`airflow-init`), поднял webserver/scheduler.

Скрыпник Михаил: Создал `airflow/dags/flight_pipeline.py` (задачи, зависимости, retry), настроил Variable `EVAL_THRESHOLD` и ручной запуск DAG.

Беспалый Максим: Подготовил отчёт по лабораторной работе. Систематизировал результаты, описал выполненные этапы, запустил `src/train.py` через DAG, проверил сохранение `models/detox_model` и токенизатора.

Провков Иван: Проверил наличие `data/raw/data.csv` или добавил загрузку, обеспечил корректный вывод `data/processed/train.csv`, `test.csv`.

Власюк Данил: Запустил `src/evaluate_t5.py` и проверил создание `reports/eval.json`, настроил порог и подтвердил регистрацию (или падение при недостачном BLEU).